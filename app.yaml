runtime: python39

entrypoint: gunicorn -b :$PORT app:app

handlers:
# Serve static files directly for maximum server speed
- url: /static
  static_dir: static

# Route all other requests to the Flask server
- url: /.*
  script: auto
